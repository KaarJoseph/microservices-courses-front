// Clase del frontend (TypeScript)
export class Course {
  id: number = 0;
  name: string = ''; // Mapeado al campo 'name' en el backend
  teacher: string = ''; // Mapeado al campo 'teacher' en el backend
}