// Clase del frontend (TypeScript)
export class Student {
    id: number = 0;
    name: string = ''; // Mapeado al campo 'name' en el backend
    lastname: string = ''; // Mapeado al campo 'lastname' en el backend
    email: string = ''; // Mapeado al campo 'email' en el backend
    password: string = ''; // Mapeado al campo 'password' en el backend
    courseId: number = 0; // Mapeado al campo 'courseId' en el backend
  }
  